function openPopup(popupId) {
    document.getElementById(popupId).style.display = 'flex'; /* Pop-up muncul sebagai flexbox */
}
 
function closePopup(popupId) {
    document.getElementById(popupId).style.display = 'none'; /* Sembunyikan pop-up */
}